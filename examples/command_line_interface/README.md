## Readme

This directory contains examples for the command line interface (CLI) of *pyHMT2D*, i.e., directly using commands in a Windows terminal.

Make sure you are in the Python virtual environment where *pyHMT2D* is installed. Then, navigate to the example directory, for example:

```bash
cd ras_to_srh
```

See further instructions in each of the examples.

